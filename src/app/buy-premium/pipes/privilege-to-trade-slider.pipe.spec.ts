import { PrivilegeToTradeSliderPipe } from './privilege-to-trade-slider.pipe';

describe('PrivilegeToTradeSliderPipe', () => {
  it('create an instance', () => {
    const pipe = new PrivilegeToTradeSliderPipe();
    expect(pipe).toBeTruthy();
  });
});
