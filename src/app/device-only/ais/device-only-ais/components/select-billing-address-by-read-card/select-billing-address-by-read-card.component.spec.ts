import { SelectBillingAddressByReadCardComponent } from './select-billing-address-by-read-card.component';

describe('SelectBillingAddressComponent', () => {
  let component: SelectBillingAddressByReadCardComponent;

  beforeEach(() => {
    component = new SelectBillingAddressByReadCardComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
